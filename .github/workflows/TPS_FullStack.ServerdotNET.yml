on:
  push:
    branches:
      -  develop
jobs:
  my_job:
    name: testing
    runs-on: windows-2022
    steps:
      - name: Build .NET Core
        run: |
          dotnet restore
          dotnet build
